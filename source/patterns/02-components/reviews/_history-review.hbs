<div class="review" id="review{{reviewID}}">
  <p class="review-meta">
    {{#if featured}}
      <strong class="u-block">This is a Featured Review!</strong>
    {{/if}}
    <span class="review-date">{{date}}</span>
    <span class="hidden-xs"> with </span>
    <span class="reviewed"><a href="/pages/advisor-bio/01-advisor-bio.html">{{reviewed}} x{{ext}}</a></span>
    <span class="review-stars stars stars-{{replace rating "." "-"}}"><span class="sr-only">{{{formatFraction rating}}} stars</span></span>
    <span class="review-type">
      {{#if call}}
        Phone Reading
      {{/if}}
      {{#if chat}}
        Chat Reading
      {{/if}}
    </span>
  </p>
  <p class="review-content">
    {{review}}
  </p>
  <p> 
    {{#if note}}
      <a href="#add-note-block-{{reviewID}}" data-toggle="collapse" class="btn btn-default btn-shaded collapsed">
        See Your Reading Note
      </a>
    {{else}}
      <a href="#add-note-block-{{reviewID}}" data-toggle="collapse" class="btn btn-default btn-shaded collapsed">
        Add a Reading Note
      </a>
    {{/if}}
  </p>
  <div id="add-note-block-{{reviewID}}" class="collapse box box-lighter box-teal">
    <p>Your reading notes will be saved in your Review History for up to 90 days. You can edit them at anytime.</p>
    <div class="form-group u-margin-bottom">
      <textarea class="form-control" rows="5" name="reading-note">{{#if note}}{{note}}{{/if}}
      </textarea>
    </div>
    <div class="arrange arrange-equal arrange-gutter">
      <div class="arrange-fill">
        <button class="btn btn-shaded btn-block js-dismiss-rating">Nevermind</button>
      </div>
      <div class="arrange-fill">
        <button class="btn btn-primary btn-block">Edit Note</button>
      </div>
    </div>
  </div>
</div>
